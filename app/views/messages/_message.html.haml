.MessageFrame{data: {message: {id: message.id}}}
  .MessageFrame__NameDate
    .MessageFrame__NameDate__Name
      = message.user.name
    .MessageFrame__NameDate__Date
      = message.created_at.strftime("%Y年%m月%d日 %H時%M分")
  .MessageFrame__Text
    - if message.body.present?
      %p.MessageFrame__Text__Body
        = message.body
    = image_tag message.image.url, class: 'MessageFrame__Text__image' if message.image.present?